<div class="container">
	<div class="row">
		<div class="col-md-12">
		<p id="notice"><%= notice %></p>
			<div class="panel panel-default">
				<div class="panel-heading">
					<strong><%= @page.title.html_safe %></strong>
				</div>
				<div class="panel-body">
				<p>
					<%= @page.body.html_safe %>
				</p>  					
				</div>
			</div>
		<div class="well">
			 <p>
			 	<strong>Slug:</strong>
  				<%= root_path + @page.slug %> <br>
			</p>
		</div>
		<%= link_to 'Edit', edit_admin_page_path(@page) %> |
		<%= link_to 'Back', admin_pages_path %>
		</div>
	</div>
</div>
